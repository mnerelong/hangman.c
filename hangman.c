// MM CC EP Hangman
// hgey yalllll r u guys gud
#include <stdio.h>
#include <math.h>
#include <string.h>
#include <time.h>
#include <stdlib.h>

int main(void)
{
    // Meika's work
    char word[10];
    srand(time(NULL));
    int random = rand() %10 +1;
    if (random == 1){
        char* word = "build";
    } else if (random == 2) {
        char* word = "lovely";
    } else if (random == 3) {
        char* word = "game";
    } else if (random == 4) {
        char* word = "first";
    } else {
        char* word = "five";
    }

    printf("%s\n", word);

    char user_vari;
    char chosen_word =
        // Elias worked on
        if (user_vari)

        else (hp - 1)
        print
    {
        printf("You can vote!\n")
    }
    if (hp < 0)
    {
        printf(" you can go to school! :(\n");
    }
    else
    {
        printf(":( GAME OVER, YOU FAILED, LOSE, GIT GUD, L, BOO HOO ):\n");
    }

    // Charlie worked on
     if ( < ) {
        printf("\n");
    } else if ( >= ) {
        printf("\n");
    } else if ( >= ) {
        printf("\n");
    } else if ( >= ) {
        printf("\n");
    } else if ( >= ) {
        printf("\n");
    } else {
        printf("\n");
    }

    return 0;
}